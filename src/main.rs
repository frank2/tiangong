fn main() {
    /* connect to host and port
     * if connect fails, bail
     * detect operating system
       * if Windows, cmd.exe
       * else, bash
     * redirect stdin to socket
     * redirect stdout/stderr to socket
     * launch shell
     * while running, check if socket is alive
     * close socket */
}
